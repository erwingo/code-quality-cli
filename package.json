{
  "name": "code-quality-cli",
  "version": "0.0.1",
  "description": "Code quality cli tools that will help you to keep your code organized and clean.",
  "main": "src/index.js",
  "scripts": {
    "test": "npm run clean:ds_store && npm run lint:js && npm run test:coverage",
    "test:coverage": "nyc --reporter=html --reporter=text mocha ./src/**/_tests/index.js",
    "clean:ds_store": "find . -name .DS_Store -delete",
    "lint:js": "eslint ./src"
  },
  "author": "Erwin Gaitan O <erwingaitano@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "mocha": "^3.4.2",
    "nyc": "^11.0.2"
  },
  "dependencies": {
    "commander": "^2.10.0",
    "fs-extra": "^3.0.1",
    "glob": "^7.1.2",
    "node-helpers": "git+https://github.com/erwingaitano/node-helpers.git"
  }
}
